# Bloque tout accès HTTP direct aux fichiers stockés ici : ils ne doivent être servis
# que par download.php, après vérification du secret partagé.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
